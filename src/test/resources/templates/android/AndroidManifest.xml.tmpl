<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.touchableheroes.android.mrcounter"
    android:versionCode="{{app.versionCode}}"
    android:versionName="{{app.versionName}}" >

    <uses-sdk
        android:minSdkVersion="{{app.AndroidSdk.min}}"
        android:targetSdkVersion="{{app.AndroidSdk.target}}" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
    </application>

</manifest>
